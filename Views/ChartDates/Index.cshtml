@model IEnumerable<Seating.Models.ChartDate>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.URLDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ChartURL)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.URLDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ChartURL)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ChartID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ChartID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ChartID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
